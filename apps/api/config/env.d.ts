export declare const env: {
  db: {
    host: string;
    port: number;
    user: string;
    password: string;
    name: string;
  };
  api: {
    port: number;
  };
  email: {
    host: string;
    port: number;
    user: string;
    password: string;
  };
  origins: {
    client: string;
  };
  google: {
    apiKey: string;
  };
};
//# sourceMappingURL=env.d.ts.map
